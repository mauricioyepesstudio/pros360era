# EvoluSA Handoff — 2026-09-04

## PROJECT
- Repo: C:\Projects\pros360era
- Branch: main
- Vercel: project "evolusa", domain evolusa.vercel.app, team_wY2WBOjgilrmaTGoab7TO7HV
- Supabase project ref: ovialqdazxkekvqqgdiu

## STATUS
- Duplicate-photo bug fixed and pushed (protection.jpg == stability.jpg, reverted to placeholder). Live.
- Stripe connection-fee flow ($25/connection) fully built and verified working in Sandbox mode.
- NOTARY category active in production (migration 0013 applied).

## CURRENT PROBLEM — IN PROGRESS, NOT DONE
Switching Stripe from Sandbox to Live mode so the $25 connection fee charges real cards. Confirmed along the way:
- The Stripe account ("Auto Flow Systems") is already activated for live payments — no business/bank verification needed, pk_live key already exists.
- Reached the "Reveal live key" screen in Stripe Dashboard → Developers → API keys.

## NEXT ACTION (exactly one)
Reveal the live Secret key (`sk_live_...`) in Stripe Dashboard → Developers → API keys, then paste it into Vercel → evolusa project → Settings → Environment Variables → `STRIPE_SECRET_KEY` (confirm "Environment" includes Production). Then create a **live-mode** webhook (Developers → Webhooks → Add destination) pointing to `https://evolusa.vercel.app/api/stripe/webhook`, event `checkout.session.completed`, and paste its `whsec_...` into `STRIPE_WEBHOOK_SECRET` in Vercel the same way.

Neither secret should ever be pasted into chat with Claude — the user pastes both directly into Vercel.

## DO NOT TOUCH
- .env, .env.local
- Supabase credentials
- Vercel credentials/configuration
- Git remotes

## PENDING (not blocking)
- Marie Fernández (notary) has no auth.users account yet — she must sign up at evolusa.vercel.app/signup with marieferqueen@gmail.com before her professional_verifications row can be created.
- Two marketing creatives still need real photos only the user can supply (himself, and Daniela Torres with her authorization) — see the launch-plan artifact.

## GIT
- Branch: main
- State: clean, in sync with origin/main
- Latest commit: 9db7ded fix(evolusa): revert duplicate protection photo to placeholder
