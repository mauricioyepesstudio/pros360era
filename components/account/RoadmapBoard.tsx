import { CheckCircle2, CircleDot, Clock3, Telescope } from "lucide-react";
import type { Recommendation, RoadmapOutput } from "@/data/account/types";
const columns = [{key:"completed",label:"Completado",icon:CheckCircle2,color:"text-[var(--success)]"},{key:"now",label:"Ahora",icon:CircleDot,color:"text-[var(--brand-coral)]"},{key:"upcoming",label:"Próximamente",icon:Clock3,color:"text-[var(--brand-blue)]"},{key:"later",label:"Más adelante",icon:Telescope,color:"text-[var(--muted)]"}] as const;
const sourceLabel = { OFFICIAL:"Oficial", EVOLUSA_GUIDE:"Guía EVOLUSA", PROFESSIONAL:"Profesional" } as const;
function Item({item}:{item:Recommendation}) { return <article className="rounded-[var(--radius-md)] border border-[var(--border)] bg-white p-4"><div className="flex items-center justify-between gap-2"><span className="text-[11px] font-bold uppercase tracking-wider text-[var(--brand-blue)]">{sourceLabel[item.sourceType]}</span>{item.requiresProfessional&&<span className="rounded-full bg-rose-50 px-2 py-1 text-[10px] font-bold text-rose-700">Requiere profesional</span>}</div><h3 className="mt-3 font-bold text-[var(--brand-navy)]">{item.title}</h3><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{item.description}</p><p className="mt-3 text-xs text-slate-500">{item.reason}</p></article> }
export default function RoadmapBoard({roadmap,compact=false}:{roadmap:RoadmapOutput;compact?:boolean}) { const shown=compact?columns.slice(0,3):columns; return <div className={compact?"grid gap-4 lg:grid-cols-3":"grid gap-5 md:grid-cols-2 xl:grid-cols-4"}>{shown.map(({key,label,icon:Icon,color})=><section key={key} aria-labelledby={`roadmap-${key}`} className="rounded-[var(--radius-lg)] bg-[var(--sky-surface)] p-4"><div className="flex items-center gap-2"><Icon aria-hidden className={color} size={19}/><h2 id={`roadmap-${key}`} className="text-sm font-bold uppercase tracking-wider text-[var(--brand-navy)]">{label}</h2></div><div className="mt-4 space-y-3">{roadmap[key].length?roadmap[key].map((item)=><Item key={item.id} item={item}/>):<p className="rounded-[var(--radius-md)] border border-dashed border-[var(--border)] p-4 text-sm text-[var(--muted)]">No hay acciones en este grupo todavía.</p>}</div></section>)}</div> }
