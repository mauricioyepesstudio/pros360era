"use client";
import { useState } from "react";
import { Check, RefreshCw } from "lucide-react";
import { lifeEvents } from "@/data/account/foundation";
import { cn } from "@/lib/cn";
export default function LifeEvents() { const [selected,setSelected]=useState<string[]>([]); return <section className="rounded-[var(--radius-lg)] border border-[var(--border)] bg-white p-5 sm:p-6"><div className="flex items-start gap-3"><span className="flex size-10 shrink-0 items-center justify-center rounded-full bg-blue-50 text-[var(--brand-blue)]"><RefreshCw aria-hidden size={19}/></span><div><h2 className="text-xl font-bold text-[var(--brand-navy)]">¿Cambió algo en tu vida?</h2><p className="mt-1 text-sm leading-6 text-[var(--muted)]">Los cambios que elijas podrán ajustar tus recomendaciones. No guardamos esta selección en Fase 1.</p></div></div><div className="mt-5 flex flex-wrap gap-2">{lifeEvents.map((event)=>{const active=selected.includes(event.id);return <button key={event.id} type="button" aria-pressed={active} onClick={()=>setSelected((current)=>active?current.filter((id)=>id!==event.id):[...current,event.id])} className={cn("inline-flex min-h-11 items-center gap-2 rounded-full border px-4 py-2 text-left text-sm font-semibold transition",active?"border-[var(--brand-blue)] bg-[var(--brand-navy)] text-white":"border-[var(--border)] bg-white text-[var(--brand-navy)] hover:border-[var(--brand-blue)]")}>{active&&<Check aria-hidden size={15}/>} {event.label}</button>})}</div>{selected.length>0&&<p className="mt-4 text-sm font-semibold text-[var(--success)]" role="status">Vista preliminar actualizada con {selected.length} cambio{selected.length===1?"":"s"}.</p>}</section> }
