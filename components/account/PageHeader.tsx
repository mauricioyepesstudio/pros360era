export default function PageHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <header><p className="text-xs font-bold uppercase tracking-[0.16em] text-[var(--brand-blue)]">{eyebrow}</p><h1 className="mt-3 text-balance text-3xl font-extrabold tracking-tight text-[var(--brand-navy)] sm:text-4xl">{title}</h1><p className="mt-4 max-w-3xl text-base leading-7 text-[var(--muted)] sm:text-lg">{description}</p></header>; }
