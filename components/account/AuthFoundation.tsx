import Link from "next/link";
import { LockKeyhole } from "lucide-react";
import { getAuthReadiness } from "@/lib/auth/config";
export default function AuthFoundation({mode}:{mode:"login"|"signup"}) { const readiness=getAuthReadiness(); const signup=mode==="signup"; return <div className="mx-auto w-full max-w-md rounded-[var(--radius-lg)] border border-[var(--border)] bg-white p-6 shadow-[var(--shadow-md)] sm:p-8"><span className="flex size-12 items-center justify-center rounded-full bg-blue-50 text-[var(--brand-blue)]"><LockKeyhole aria-hidden/></span><h1 className="mt-6 text-3xl font-extrabold text-[var(--brand-navy)]">{signup?"Crea tu cuenta gratis":"Entra a tu cuenta"}</h1><p className="mt-3 leading-7 text-[var(--muted)]">{signup?"Guarda tu diagnóstico y continúa tu camino personalizado.":"Continúa tu Roadmap y revisa tu progreso."}</p><div className="mt-6 rounded-[var(--radius-md)] border border-blue-200 bg-blue-50 p-4"><p className="font-bold text-[var(--brand-navy)]">Autenticación pendiente de configuración</p><p className="mt-2 text-sm leading-6 text-[var(--muted)]">No creamos sesiones falsas. La interfaz está preparada para email y contraseña cuando Supabase Auth esté conectado.</p>{!readiness.configured&&<p className="mt-2 text-xs text-slate-600">Faltan: {readiness.missing.join(", ")}</p>}</div><div className="mt-6 space-y-4"><label className="block text-sm font-semibold text-[var(--brand-navy)]">Correo electrónico<input disabled type="email" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-slate-50 px-4" placeholder="tu@correo.com"/></label><label className="block text-sm font-semibold text-[var(--brand-navy)]">Contraseña<input disabled type="password" className="mt-2 min-h-12 w-full rounded-xl border border-[var(--border)] bg-slate-50 px-4" placeholder="••••••••"/></label><button disabled className="min-h-12 w-full rounded-full bg-slate-300 font-bold text-slate-600">{signup?"Crear cuenta":"Entrar"}</button></div><p className="mt-6 text-center text-sm text-[var(--muted)]">{signup?"¿Ya tienes cuenta?":"¿Aún no tienes cuenta?"} <Link className="font-bold text-[var(--brand-blue)]" href={signup?"/login":"/signup"}>{signup?"Entrar":"Crear cuenta"}</Link></p></div> }
